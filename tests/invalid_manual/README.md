Invalid compile testing

Due to rustc version specific errors and line numbers changing all the time,
these tests of wrongly invoking the compiler are done in a manual fashion
instead of using trybuild.

